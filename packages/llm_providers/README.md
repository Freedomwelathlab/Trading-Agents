# llm_providers (placeholder)

Destination for the LLM provider abstraction ported from TradingAgents'
`llm_clients/` (Apache-2.0 — see root `NOTICE`). Not yet populated; will be
built in the phase that adds the analysis/agent layer, after the risk engine
and execution boundary exist.
